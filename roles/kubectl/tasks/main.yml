---
# tasks file for kubectl
- name: Install kubectl
  apt:
    name: kubectl={{ kubectl_version }}
    state: present
    update_cache: yes
  tags: install

- name: Hold kubectl
  dpkg_selections:
    name: kubectl
    selection: hold